{
	"name": "fontview",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"forwardPorts": [
		5901
	],
	"overrideCommand": false,
	"customizations": {
		"vscode": {
			"extensions": [
				"rust-lang.rust-analyzer",
				"vadimcn.vscode-lldb",
				"swellaby.vscode-rust-test-adapter",
				"github.vscode-github-actions"
			]
		}
	},
	"onCreateCommand": "git config user.name $(git --no-pager log --pretty=\"%an\" | head -n 1) && git config user.email $(git --no-pager log --pretty=\"%ce\" | head -n 1) && cargo clean && cargo build --features print",
	"containerEnv": {
		"DISPLAY": ":1",
		"VNCPASS": "font"
	}
}